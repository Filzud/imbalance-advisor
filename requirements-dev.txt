# Dev / экспериментальные зависимости (не нужны для прод-инференса инструмента).
# Устанавливать поверх runtime: pip install -r requirements.txt -r requirements-dev.txt

# --- подбор гиперпараметров (модуль 4) ---
optuna==3.6.1

# --- опциональный UI (модуль 6) ---
streamlit==1.37.0

# --- тесты и покрытие ---
pytest==8.3.2
pytest-cov==5.0.0

# --- линтер / форматирование (PEP-8) ---
ruff==0.5.6
